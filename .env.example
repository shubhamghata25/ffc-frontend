# ── REQUIRED: Your Render backend URL (no trailing slash) ─────────────────────
VITE_API_URL=https://ffc-backend.onrender.com

# ── KIOSK: Same value as KIOSK_TOKEN in your Render backend env vars ──────────
# Generate once: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
VITE_KIOSK_TOKEN=REPLACE_WITH_YOUR_KIOSK_TOKEN

# ── PAYMENTS ──────────────────────────────────────────────────────────────────
VITE_RAZORPAY_KEY_ID=rzp_live_XXXXXXXXXXXX

# ── OPTIONAL ──────────────────────────────────────────────────────────────────
VITE_PHONEPE_UPI_ID=friendsfitnessclub@ybl

# REMOVED: VITE_ADMIN_SECRET — admin auth is now JWT-based (server-side only)
